# make sure that docker is installed and logged in 
# run the following commands in the directory where docker-compose.yml is present

pip3 install docker-compose

docker-compose pull

#this process may take a while

docker-compose up

